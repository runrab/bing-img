name: Daily Push

on:
  schedule:
    - cron: '0 0 * * *' # 每天0点推送
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Set up Git
      run: |
        git config --global user.name "runrab"
        git config --global user.email "${{ secrets.email }}"
        echo ${{ secrets.email }}


    - name: Download Image
      run: |
        mkdir -p image
        curl -L -o image/$(date +"%Y%m%d").jpg https://source.unsplash.com/1920x1080/?landscape
        git remote add upstream https://runrab@github.com/runrab/bing-img.git
        # git config --global user.name "${{ secrets.username }}"
        # git config --global user.email "${{ secrets.email }}"
        git add image/$(date +"%Y%m%d").jpg
        git commit -m "Update image - $(date +"%Y-%m-%d")"
        git push origin HEAD
      env:
        GITHUB_TOKEN: ${{ secrets.token }}

#     - name: Update URL
#       run: |
#         echo "https://raw.githubusercontent.com/${{ secrets.username }}/${{ secrets.repo }}/main/image/$(date +"%Y%m%d").jpg" >> url.txt
#         if [ `cat url.txt | wc -l` -gt 366 ]; then sed -i '1d' url.txt; fi
#         git add url.txt
#         git commit -m "Update URL - $(date +"%Y-%m-%d")"
#         git push origin HEAD
#       env:
#         GITHUB_TOKEN: ${{ secrets.token }}
